# TODO / backlog

## Done ✅

- **Public Wi-Fi scenario** (tabbed) with a Wi-Fi router hop and Wi-Fi security
  (Open / WPA2·WPA3).
- **Sniffer / eavesdropper mode** — passive, per-link capture window with a
  detective cursor; reuses the per-segment encryption logic.
- **Fake AP** man-in-the-middle — real-AP ghost + attacker, WPA2 bypass, and the
  "a VPN still protects you" moment.
- **"What each layer protects"** and **"Can a VPN really protect you?"** panels.
- **Guess-and-reveal identity quiz** ("Can this website identify you?").

## Backlog ideas

- A short **scenario preset menu** (e.g. *public Wi-Fi + HTTP login → password
  exposed*) as one-click starting points.
- Optional **packet-inspection popup** showing a mock HTTP request vs an
  encrypted blob.
- A **print / worksheet mode** for classroom handouts.
- (Maybe) a **website-LAN sniffer** to make the "HTTPS protects transit only"
  point — deferred for now to keep the diagram calm.
